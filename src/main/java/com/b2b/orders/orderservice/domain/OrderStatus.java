package com.b2b.orders.orderservice.domain;

/**
 * For enum the status for an order
 */
public enum OrderStatus {
        CREATED,
        CONFIRMED,
        PAID,
        SHIPPED,
        CANCELLED
}
